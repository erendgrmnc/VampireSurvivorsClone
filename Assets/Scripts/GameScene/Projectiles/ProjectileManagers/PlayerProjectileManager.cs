using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerProjectileManager : ProjectileManager
{
    // Start is called before the first frame update
    void Start()
    {
        InitPool();
    }
}
